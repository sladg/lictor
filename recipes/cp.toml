# the destination is written and created; the sources are only read
[[cmd]]
name = "cp"
[[cmd.args]]
slots = "except-last"
kind = "pathset"
effect = ["read"]
recursive_with = ["-r", "-R", "--recursive", "-a", "--archive"]
[[cmd.args]]
slots = "last"
kind = "path"
effect = ["write", "create"]
